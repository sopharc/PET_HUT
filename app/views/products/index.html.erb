<h1>Product#index</h1>

<%= select_tag "products", options_from_collection_for_select(@products, "id", "category"), include_blank: true %>

<% @products.each do |product| %>
  <div>
    <%= link_to product.name, product %>
  </div>
<% end %>
