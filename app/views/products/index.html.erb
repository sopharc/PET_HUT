<h1>Product#index</h1>

<%= select_tag "products", options_for_select(@products.map {|p| [ p.category ]}.uniq, "category"), include_blank: true %>

<% @products.each do |product| %>
  <div>
    <%= link_to product.name, product %>
  </div>
<% end %>

<!-- Car.all.map {|p| [ p.year ]}.uniq, -->
